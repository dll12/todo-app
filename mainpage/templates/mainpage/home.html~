{% extends 'mainpage/base.html' %}

{% block content %}

<center>
		<h1>
TODO List
		</h1>
<br>
<form action='{% url 'createtodo' %}'>
		{% csrf_token %}
		<button class="btn btn-dark" type="submit">Create New Todo</button>
</form>
</center>
<br>

<ul>
		{% for todo in todos %}

		<li>
				{% if todo.important == True %}

				<a class="btn btn-danger" href="{{ todo.id }}">{{ todo.title }}</a>

				{% else %}

				<a class="btn btn-success" href="{{ todo.id }}">{{ todo.title }}</a>

				{% endif %}

				{% if todo.memo %}
				- 
				{{ todo.memo }}
				<br>
				<br>

				{% endif %}

		</li>

		{% endfor %}
</ul>
{% endblock %}
